nFreq = 36;
simLen = 76000;

I2input = genI2input(nFreq,simLen);

netcon=eye(N_pre,N_post)

g_preI2 = 0.1
E_exc = 0
tauR = 1
tauD = 3

% iI2(X,t) = I2input(ceil((t+dt*0.01)/dt),:);

iI2(X,t) = g_preI2 .* I2input(ceil((t+dt*0.01)/dt),:) * netcon .* (X - E_exc);


monitor iI2

@isyn += iI2(X,t)
